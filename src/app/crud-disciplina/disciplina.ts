export class Disciplina {
    nome: string;
    descricao: string;
    area: string;

    constructor(nome: string, descricao: string, area: string) {
        this.nome = nome;
        this.descricao = descricao;
        this.area = area;
    }
}